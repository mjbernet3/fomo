import 'package:flutter/material.dart';

class TempComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

//TODO: Delete this once there is another component in folder
