name: project_fomo
description: Music event hub dedicated to Atlanta.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.4.3+2 # dependency for Firebase
  cloud_firestore: ^0.13.0+1 # Firebase plugin

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
